<%= render 'users/list', user: @user, book: @book %>


<h2>Users</h2>
<% @users.each do |user| %>
<div>

  <p>Name：<%= user.name %></p>
  <p><%= link_to 'Show', user_path(user.id) %></p>

  </div>
<% end %>