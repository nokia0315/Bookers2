<h1>編集画面</h1>
<!-- form_with部分 -->
<%= form_with model:@book, local: true do |f| %>
  <h4>タイトル</h4>
  <%= f.text_field :title, autofocus: true, id:"inputTitle", placeholder:"Title" %>
  <h4>カテゴリー</h4>
  <%= f.text_field :body, autofocus: true, id:"inputBody", placeholder:"Body" %>

  <%= f.submit '保存' %>
<% end %>